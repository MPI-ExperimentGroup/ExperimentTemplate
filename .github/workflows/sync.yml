name: repository-sync
on: 
  push
jobs:
  build:
    runs-on: ubuntu-latest
    environment: repository-sync
    steps:
    - uses: actions/checkout@v3
    - run: git push https://${{ secrets.access_user }}:${{ secrets.access_token }}@${{ secrets.mirror_destination }}
        