name: repository-sync
on: 
  push
jobs:
  build:
    runs-on: ubuntu-latest
    environment: repository-sync
    steps:
    - uses: actions/checkout@v3
    - run: git push ${{ secrets.mirror_destination }}
      with:
        token: ${{ secrets.access_token }}
        